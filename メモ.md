# 社長メモ

DINKsのトリセツの追加CSSを以下に全置換してください。
変更点：① テーブルヘッダーを控えめに ② ヘッダー背景をbodyと統一

---

/* ============================================
   DINKsのトリセツ — SWELL カスタムCSS

   適用方法：
   WordPress管理画面 → 外観 → カスタマイズ → 追加CSS
   に貼り付けて保存する
   ============================================ */

/* ============================================
   1. CSS変数（カラーパレット）
   ============================================ */
:root {
  --dinks-primary: #5B8C5A;
  --dinks-primary-dark: #3D6B3C;
  --dinks-primary-light: #E8F0E8;
  --dinks-accent: #E8927C;
  --dinks-accent-light: #FFF0EC;
  --dinks-sub-accent: #D4A843;
  --dinks-bg: #FDFBF7;
  --dinks-surface: #FFFFFF;
  --dinks-text: #333333;
  --dinks-text-light: #666666;
  --dinks-border: #E5E0DA;
  --dinks-radius: 8px;
}

/* ============================================
   2. 全体の背景・テキスト
   ============================================ */
body {
  background-color: var(--dinks-bg);
  color: var(--dinks-text);
}

/* ============================================
   3. ヘッダー
   ============================================ */
/* ヘッダー上部バー（キャッチフレーズ）— フッターと同じ緑に統一 */
.l-header__bar {
  background: var(--dinks-primary-dark) !important;
}

.l-header {
  background-color: var(--dinks-bg);
  box-shadow: 0 1px 4px rgba(0, 0, 0, 0.06);
}

/* サイトタイトル */
.c-headLogo__txt,
.c-headLogo a {
  color: var(--dinks-primary-dark);
  font-weight: 900;
  letter-spacing: 0.02em;
}

/* キャッチフレーズ */
.c-headLogo__desc {
  color: var(--dinks-text-light);
  font-size: 0.75rem;
}

/* グローバルナビ */
.c-gnav a {
  color: var(--dinks-text);
  font-weight: 500;
  transition: color 0.2s;
}
.c-gnav a:hover {
  color: var(--dinks-primary);
}
.c-gnav .c-gnav__item.current a {
  color: var(--dinks-primary);
  border-bottom: 2px solid var(--dinks-primary);
}

/* ============================================
   4. 記事カード（トップページ一覧）
   ============================================ */
.p-postList__item {
  background-color: var(--dinks-surface);
  border-radius: var(--dinks-radius);
  overflow: hidden;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
  transition: transform 0.2s, box-shadow 0.2s;
}
.p-postList__item:hover {
  transform: translateY(-3px);
  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
}

/* 記事タイトル（カード内） */
.p-postList__title {
  color: var(--dinks-text);
  font-weight: 700;
  line-height: 1.5;
}

/* カテゴリラベル */
.c-categoryBadge,
.c-postThumb__cat {
  background-color: var(--dinks-primary);
  color: #fff;
  font-size: 0.7rem;
  font-weight: 600;
  border-radius: 3px;
}

/* 日付 */
.p-postList__date {
  color: var(--dinks-text-light);
  font-size: 0.8rem;
}

/* ============================================
   5. 記事本文
   ============================================ */
.post_content {
  line-height: 1.9;
  letter-spacing: 0.02em;
}

.post_content p {
  margin-bottom: 1.6em;
}

/* H2 見出し — 左ボーダー＋背景 */
.post_content h2 {
  background-color: var(--dinks-primary-light);
  border-left: 5px solid var(--dinks-primary);
  padding: 0.8em 1em;
  margin: 2.5em 0 1.2em;
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--dinks-text);
  border-radius: 0 var(--dinks-radius) var(--dinks-radius) 0;
}

/* H3 見出し — 下線アクセント */
.post_content h3 {
  border-bottom: 3px solid var(--dinks-accent);
  padding-bottom: 0.4em;
  margin: 2em 0 1em;
  font-size: 1.2rem;
  font-weight: 700;
  color: var(--dinks-text);
}

/* H4 見出し */
.post_content h4 {
  color: var(--dinks-primary-dark);
  font-weight: 700;
  margin: 1.5em 0 0.8em;
}

/* テーブル — 控えめなスタイル（H2より目立たせない） */
.post_content table {
  border-color: var(--dinks-border) !important;
}
.post_content table th,
.post_content table td {
  border-color: var(--dinks-border) !important;
}
.post_content table th {
  background-color: #f5f5f5 !important;
  color: var(--dinks-text) !important;
  font-weight: 700;
}

/* リンク */
.post_content a {
  color: var(--dinks-primary);
  text-decoration: underline;
  text-decoration-color: var(--dinks-primary-light);
  text-underline-offset: 3px;
  transition: color 0.2s, text-decoration-color 0.2s;
}
.post_content a:hover {
  color: var(--dinks-accent);
  text-decoration-color: var(--dinks-accent);
}

/* リスト */
.post_content ul li::marker {
  color: var(--dinks-primary);
}
.post_content ol li::marker {
  color: var(--dinks-primary);
  font-weight: 700;
}

/* ボックス装飾（SWELLのブロック） */
.swell-block-box {
  border-radius: var(--dinks-radius);
}

/* ============================================
   6. ふきだし
   ============================================ */
/* ふきだし全体の余白 */
.swell-block-balloon {
  margin: 1.5em 0;
}

/* ============================================
   7. ボタン（CTA）
   ============================================ */
.swell-block-button a,
.wp-block-button__link {
  background-color: var(--dinks-accent);
  color: #fff;
  border-radius: 30px;
  font-weight: 700;
  padding: 0.8em 2em;
  transition: background-color 0.2s, transform 0.1s;
  box-shadow: 0 3px 10px rgba(232, 146, 124, 0.3);
}
.swell-block-button a:hover,
.wp-block-button__link:hover {
  background-color: #D4765E;
  transform: translateY(-1px);
}

/* ============================================
   8. サイドバー（使う場合）
   ============================================ */
.l-sidebar .widget {
  background-color: var(--dinks-surface);
  border-radius: var(--dinks-radius);
  padding: 1.5rem;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
}
.l-sidebar .widget-title {
  color: var(--dinks-primary-dark);
  font-weight: 700;
  border-bottom: 2px solid var(--dinks-primary);
  padding-bottom: 0.3em;
  margin-bottom: 1em;
  font-size: 1rem;
}

/* ============================================
   9. フッター
   ============================================ */
.l-footer {
  background-color: var(--dinks-primary-dark);
  color: rgba(255, 255, 255, 0.85);
}
.l-footer a {
  color: rgba(255, 255, 255, 0.85);
  transition: color 0.2s;
}
.l-footer a:hover {
  color: var(--dinks-accent);
}
.copyright {
  color: rgba(255, 255, 255, 0.6);
  font-size: 0.8rem;
}

/* ============================================
   10. 目次（SWELL標準）
   ============================================ */
.p-toc {
  background-color: var(--dinks-primary-light);
  border: none;
  border-radius: var(--dinks-radius);
  padding: 1.5rem 2rem;
}
.p-toc__ttl {
  color: var(--dinks-primary-dark);
  font-weight: 700;
}
.p-toc a {
  color: var(--dinks-text);
}
.p-toc a:hover {
  color: var(--dinks-primary);
}

/* ============================================
   11. 関連記事・記事下エリア
   ============================================ */
.p-relatedPosts__title {
  color: var(--dinks-primary-dark);
  font-weight: 700;
}

/* SNSシェアボタンエリア */
.c-shareBtns {
  margin: 2.5em 0;
}

/* ============================================
   12. モバイル対応
   ============================================ */
@media (max-width: 599px) {
  .post_content h2 {
    font-size: 1.2rem;
    padding: 0.6em 0.8em;
  }
  .post_content h3 {
    font-size: 1.1rem;
  }
  .post_content {
    line-height: 1.85;
  }
}

/* ============================================
   13. タグ・カテゴリページ
   ============================================ */
.c-tagList a {
  background-color: var(--dinks-primary-light);
  color: var(--dinks-primary-dark);
  border-radius: 20px;
  padding: 0.3em 0.8em;
  font-size: 0.8rem;
  transition: background-color 0.2s;
}
.c-tagList a:hover {
  background-color: var(--dinks-primary);
  color: #fff;
}

/* ============================================
   14. ページネーション
   ============================================ */
.c-pagination .page-numbers {
  border-radius: var(--dinks-radius);
}
.c-pagination .page-numbers.current {
  background-color: var(--dinks-primary);
  color: #fff;
}

/* ============================================
   15. ブログカード（内部リンク）
   ============================================ */
.p-blogCard {
  border: 1px solid var(--dinks-border);
  border-radius: var(--dinks-radius);
  transition: box-shadow 0.2s;
}
.p-blogCard:hover {
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
}

/* ============================================
   16. スクロールバー（Webkit）
   ============================================ */
::-webkit-scrollbar {
  width: 8px;
}
::-webkit-scrollbar-track {
  background: var(--dinks-bg);
}
::-webkit-scrollbar-thumb {
  background: var(--dinks-border);
  border-radius: 4px;
}
::-webkit-scrollbar-thumb:hover {
  background: var(--dinks-text-light);
}

/* ============================================
   17. セレクション（テキスト選択時の色）
   ============================================ */
::selection {
  background-color: var(--dinks-primary-light);
  color: var(--dinks-text);
}
